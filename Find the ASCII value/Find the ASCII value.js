// Write a code to find Find the ASCII value of a character
function getAsciiValue(character){
    return character.charCodeAt(0);
}
let character = 'A';
console.log(`The ASCII vlaue of ${character} is ${getAsciiValue(character)}`);
